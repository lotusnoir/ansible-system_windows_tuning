---
- name: Allow ICMPv4 Echo Request (ping)
  ansible.windows.win_shell: |
    New-NetFirewallRule `
      -DisplayName "Allow ICMPv4 Echo Request" `
      -Protocol ICMPv4 `
      -IcmpType 8 `
      -Direction Inbound `
      -Action Allow
